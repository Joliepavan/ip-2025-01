#include <bits/stdc++.h>
#include <string>

using namespace std;

int main (){
 int J;
 string S, resultado;

 cin >> J;
 
 for(int i=0; i<=J; i++){
 vector < char > p;
 cin >> S;
  for (int i=0; i<S.size()-2;i++){
    p.push_back(S[i]);
  }
 

p.push_back('i');

  for (int i=0; i<p.size();i++){
    cout << p[i];
  }
 }
    return 0;
}
